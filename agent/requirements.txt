psutil>=5.9.0
httpx>=0.27.0
uvicorn>=0.29.0
starlette>=0.36.0
# 浏览器压测引擎（二选一即可）:
# 方案1: ChromeDriver（推荐 Windows，需安装 Chrome + chromedriver.exe）
selenium>=4.15.0
# 方案2: Playwright（需额外下载 Chromium）
# pip install playwright && playwright install chromium
playwright>=1.40.0
# Windows 服务支持（仅 Windows 需要）
pywin32>=306; sys_platform == "win32"
